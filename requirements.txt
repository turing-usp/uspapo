# --- Web Scraping & Limpeza ---
scrapy==2.13.3
# Janela estreita, com um motivo em cada ponta:
#   >=24.7  Twisted mais antigo importa o modulo `cgi`, removido no Python 3.13.
#           Era o pino antigo (`Twisted<24.3.0`) que fazia `import scrapy`
#           quebrar em qualquer interpretador 3.13+ - a CI roda 3.11 e nao via,
#           mas ninguem conseguia rodar spider localmente.
#   <26     Twisted 26 removeu `_setAcceptableProtocols` de
#           `twisted.internet._sslverify`, que o scrapy 2.13.3 importa: sem ele
#           os download handlers de http e https nao carregam e o crawler nao
#           baixa nada.
Twisted>=24.7,<26
pypdf==6.14.2

# --- Banco de Dados Vetorial & IA (Cloud-Native) ---
pinecone>=3.0.0
supabase>=2.0.0
openai>=1.40.0
groq>=0.4.0
# Usado pelo embeddings/chunking.py para fatiar bloco grande sem cortar palavra.
langchain-text-splitters==1.1.2

# --- Utilitarios ---
tqdm==4.68.4
python-dotenv>=1.0.0
# Valida o token do Supabase no /chat. O extra [crypto] traz o `cryptography`,
# sem o qual nao da para conferir assinatura ES256/RS256 - e e assim que o
# Supabase assina desde as chaves novas. O >=2.9 e pelo PyJWKClientConnectionError,
# que separa "JWKS fora do ar" (503) de "token invalido" (401).
PyJWT[crypto]>=2.9.0

# --- HTTP (cardapio do bandejao) ---
requests>=2.32.0

# --- API ---
Flask==3.1.3
Flask-Cors==6.0.5
gunicorn>=21.2.0
